<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ renderData.title or title or metadata.title }}</title>
    <meta
      name="description"
      content="{{ renderData.description or description or metadata.description }}"
    />
    <meta name="theme-color" content="#ffffff">
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}" />
    <link rel="stylesheet" href="{{ '/css/dracula.css' | url }}" />
    <link
      rel="alternate"
      href="{{ metadata.feed.path | url }}"
      type="application/atom+xml"
      title="{{ metadata.title }}"
    />
    <link rel="manifest" href="{{ '/manifest.json' | url }}" />
    <script defer src="{{ '/js/index.js' | url }}"></script>
  </head>
  <body>
    <header>
      <h1><a href="/">Dan Jordan</a></h1>

      <nav>
        <a
          href="https://twitter.com/danjordan"
          title="Twitter"
          rel="noreferrer noopener"
          target="_blank">Twitter</a>
        &nbsp;|&nbsp;
        <a
          href="https://github.com/danjordan"
          title="GitHub"
          rel="noreferrer noopener"
          target="_blank">GitHub</a>
      </nav>
    </header>

    <main>{{ content | safe }}</main>

    {% set ga %} {% include "ga.js" %} {% endset %}
    <script>
      {{ ga | jsmin | safe }}
    </script>
    <script
      async
      defer
      src="https://www.google-analytics.com/analytics.js"
    ></script>
  </body>
</html>
