{% if collections.tils.length %}

<h2>Recent posts</h2>

<ul>
{% for post in collections.tils | reverse %}
  <li><a href="{{ post.url | url }}">{{ post.data.title }}</a></li>
{% endfor %}
</ul>

{% endif %}